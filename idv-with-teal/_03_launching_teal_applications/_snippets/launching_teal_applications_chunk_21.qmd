# This may cause issues
app <- teal::init(
  data = teal.data::teal_data(IRIS = iris),
  modules = teal::modules(
    teal.modules.general::tm_data_table() # Missing required 'dataname' argument
  )
)
